# Flowform Model

Flowform 自有模型、模型元数据和模型包相关定义。

M4 的第一目标是 Flowform Model V0：Past Motion + Player Intent + Trajectory Anchor → Future Motion。

大型模型权重不应直接无规则提交到 Git；后续根据发布策略使用 Git LFS 或 Release Artifact。
